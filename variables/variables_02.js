function setup() {
    createCanvas(400, 400);
}

function draw() {

    noStroke();
    fill(0, 150, 200, 175);
    circle(mouseX, mouseY, 15);
}

function mousePressed() {
    background(0, 0, 75);
}
